<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".CreatePostFragment" >

    <EditText
        android:id="@+id/editTextEnterPost"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginTop="24dp"
        android:layout_marginEnd="24dp"
        android:hint="@string/enter_post"
        android:hintTextColor="#78909C"
        android:textSize="20sp"
        android:inputType="text"
        android:maxLength="140"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="TextContrastCheck"
        android:importantForAutofill="no" />

    <Button
        android:id="@+id/buttonSubmitPost"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="@string/submit"
        android:textAllCaps="false"
        android:textSize="19sp"
        android:backgroundTint="@color/light_grey"
        android:textColor="@color/dark_grey"
        app:layout_constraintEnd_toEndOf="@+id/editTextEnterPost"
        app:layout_constraintStart_toStartOf="@+id/editTextEnterPost"
        app:layout_constraintTop_toBottomOf="@+id/editTextEnterPost" />

    <TextView
        android:id="@+id/textViewCancelNewPost"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="@string/cancel"
        android:textSize="17sp"
        android:textColor="@color/dark_grey"
        app:layout_constraintEnd_toEndOf="@+id/buttonSubmitPost"
        app:layout_constraintTop_toBottomOf="@+id/buttonSubmitPost" />
</androidx.constraintlayout.widget.ConstraintLayout>